<UserControl x:Class="SMAControlApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"      mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F3F3F3">
        <Border VerticalAlignment="Center" HorizontalAlignment="Center" 
                Background="White" CornerRadius="8" Padding="30" Width="320">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.1"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="SMA System Login" FontSize="22" FontWeight="SemiBold" 
                           HorizontalAlignment="Center" Margin="0,0,0,25" Foreground="#333"/>

                <TextBlock Text="Username" Margin="0,0,0,5" Foreground="#666" FontSize="12"/>
                <TextBox x:Name="txtUsername" Height="32" VerticalContentAlignment="Center" 
                         Padding="5,0" Margin="0,0,0,15"/>

                <TextBlock Text="Password" Margin="0,0,0,5" Foreground="#666" FontSize="12"/>
                <PasswordBox x:Name="txtPassword" Height="32" VerticalContentAlignment="Center" 
                             Padding="5,0" Margin="0,0,0,10"/>

                <TextBlock x:Name="lblError" Text="" Foreground="#D32F2F" 
                           FontSize="11" Margin="0,0,0,15" TextAlignment="Center" FontWeight="Medium"/>

                <Button x:Name="btnLogin" Content="Login" Height="35" 
                        Background="#0078D4" Foreground="White" FontWeight="Bold"
                        Cursor="Hand" Click="btnLogin_Click" IsDefault="True">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>